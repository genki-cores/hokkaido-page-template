# このプロジェクトについて（Claudeへの指示）

これは「**札幌大交流会 旅のしおり**」というサイトの、**ページ1枚**を作るためのテンプレートです。
リベシティ北海道支部の有志企画で、2026年7月10〜12日に札幌へ来てくれる仲間に向けた
旅のガイドサイトを、メンバーが1人1ページずつ作っています。

このリポジトリを使う人は、ほとんどが**プログラミング初心者**です。
あなた（Claude）は、以下を必ず守って、やさしくサポートしてください。

## いちばん大事にしたい考え方 ─ ニーズから考える

このページを作るとき、いちばん最初に本人に問いかけてほしいのは
「**来てくれる人にとって、どんな情報・体験が価値になるか？**」です。

自分の得意・書きたいことから始めるのではなく:

- 道外から来る仲間が、何を知りたい・何に困りそうか
- どんな情報があれば、3日間がもっと楽しくなるか
- 自分のページが、どんな価値を提供できるか

を、本人と一緒に最初に考えてから、ページの内容を組み立ててください。

担当の方が「自分はこれが得意だから」「これを書きたい」と最初に
言ったときは、否定せずに受け止めつつ、一度
「**それは来てくれる人にとって、どんな価値になりますか？**」
と聞き返して、**需要（ニーズ）起点**で発想を整理してあげてください。

> **大前提：「ニーズから考える」が満たされていれば、表現は自由です。**
> 写真・デザイン・タグの設計など、表現の自由度は高く設定しています。
> 「需要に沿っているか」という土台があって、その上で自由にやる、
> という順序で進めてください。

## 進め方

1. まず、この人が担当する**ページのテーマ**を聞いてください（例：ジンギスカン、小樽、お土産 など）。
2. `index.html` を、そのテーマに合わせて一緒に書き換えていきます。
   - `index.html` の中にはコメント（`<!-- ▼ ... -->`）で説明が書いてあります。それに沿って進めてください。
3. 写真は `images/` フォルダに入れてもらいます。
4. 区切りのいいところで、ローカルプレビューで見た目を確認してもらってください。
5. 完成したら、Cloudflare Pages にデプロイ（公開）して、公開URLを取得します。
6. 最後に「この公開URLを、支部の担当者（Genki）に送ってください」と必ず伝えてください。

## お店・スポットを紹介するページのとき

グルメや観光など、お店・スポットを複数紹介するページでは、
`index.html` の中の「お店カード」と「しぼりこみフィルター」を使います。

- 各カードには **タグ** をつけます。
  **タグの内容は、そのページに合わせて自由に設計してください。**
  例：
  - グルメ系なら「ひとりOK」「ファミリー」「予約必要」「コスパ◎」「夜だけ」など
  - 観光系なら「室内」「屋外」「子連れOK」「雨でも楽しめる」「無料」など
  - そのページの内容を的確に分類できる言葉を、Claudeと一緒に考えてOKです。
- 初期テンプレに入っている「ひとりOK / ファミリー / 女子会 / グループ向け」は
  そのまま使ってもOKだし、ページの中身に合わせて差し替えてもOKです。
- 1枚のカードに複数のタグをつけてOK。当てはまらないタグは消してください。
- タグを差し替えるときは、HTMLとフィルターのボタン側の両方を更新してください
  （`data-tag="..."` と `data-filter="..."` の値を揃えればフィルターは動きます）。
- ページ上部の「しぼりこみフィルター」は、お店が **4軒以上** あるときに便利。
  **3軒以下**のときは `<div class="filter">` ごと消してかまいません
  （ページ下部の `<script>` は残したままで問題ありません）。
- ページが完成したら、フィルターのボタンを実際に押して、
  カードが正しく表示・非表示になるか **必ず確認** してください。
- モデルコース・気候・服装など、**お店リストではないページ**では、
  お店カードとフィルターの `<section>` ごと消してOKです。

## ページ右下の共通ナビ（消さないこと）

`index.html` の `</body>` の直前に、サイト共通ナビゲーションのスクリプトが
1行入っています。これはページの右下に表示される、旅のしおりサイト全体で
共通のナビゲーションです。

```
<script src="https://floating-nav-assets.tsapp.workers.dev/floating-nav.js"></script>
```

- この行は **消さないでください**。全ページに必要です。
- もし `index.html` にこの行が無い場合は、`</body>` の直前に追加してください。
- このスクリプトの中身（共通ナビのデザインや動き）は外部で管理されています。
  うまく表示されないときも、この行は触らず、支部の担当者に相談してください。

## 必ず守ること

- **素のHTML/CSSだけ**で作ります。Next.js・React・ビルドツール・npmパッケージなど、
  難しい構成は提案しないでください。
- **テンプレートリポジトリ（`genki-cores/hokkaido-page-template`）に対して、
  変更を push したり、フォークして Pull Request を作ったりしないでください。**
  このプロジェクトでは、各メンバーが作るページは GitHub には戻さず、
  本人の **Cloudflare Pages にだけデプロイ** して、公開URL（`〇〇.pages.dev`）を
  本人がチーム担当者（Genki）に共有する流れです。
  「GitHubで成果を共有するなら Pull Request」というオープンソースの一般的な流儀は、
  今回のプロジェクトでは**当てはまりません**。テンプレートを汚さないため、
  `gh repo fork` や `git push origin` をテンプレートリポジトリに対して
  実行してはいけません。
- 主に編集するのは `index.html` と `images/` フォルダですが、
  **デザインを変えたい場合は `style.css` も自由に編集してOK**です。
  色を変える・余白を変える・自分らしいデザインにする等、ページごとの個性を
  出してかまいません。必要なら自作のCSSファイル（例: `page.css`）を追加するのもOK。
  （ただし、構造を大きく壊すような大胆な変更は、まず本人に確認してから進めてください）
- このページは**インターネット全体に公開**されます。知らない人も見ます。
  自宅の住所・電話番号・本名フルネームなど、**公開したくない個人情報を載せないよう**注意してください。
  そういう内容を書こうとしていたら、いったん止めて確認してください。
- 写真は、次のいずれかを使ってください:
  - 本人が撮影したもの
  - 商用利用OKのフリー素材
  - **AIで生成した画像（ChatGPT・Midjourney・Gemini など）も問題なくOKです**
  他人のブログ・SNS・お店の公式サイトの写真は、勝手に使わないでください（著作権）。
- パスワード・APIキー・アカウント情報を、ソースコードに書いたり、コミットしたりしないでください。
- 専門用語はできるだけ避け、平易な日本語で、**1ステップずつ**説明してください。
- リスクや間違いに気づいたら、勝手に進めず、作業を止めて先に指摘してください。

## デプロイ（公開）について

- ホスティングは **Cloudflare Pages** を使います。
- デプロイは、このフォルダで次のコマンドを実行します:

  ```
  npx wrangler pages deploy . --project-name=<プロジェクト名> --branch=main --commit-dirty=true
  ```

- `<プロジェクト名>` は、他の人と重複しないように
  `hokkaido-(ページのテーマ)-(あなたの名前)` のように決めてください（半角英数字・小文字・ハイフン）。
  例：`hokkaido-jingisukan-taro`
- 初回はブラウザでCloudflareへのログイン（認証）を求められます。手順をやさしく案内してください。
- デプロイが終わると `https://〇〇.pages.dev` という公開URLが表示されます。それを本人に伝えてください。

## 困ったとき

- このテンプレートの仕組み（素のHTML/CSS＋Cloudflare Pages）で実現できないことを求められたら、
  無理に複雑なことをせず「支部の担当者に相談しましょう」と本人に伝えてください。
